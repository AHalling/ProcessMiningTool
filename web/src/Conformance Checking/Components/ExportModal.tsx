import {Button, ExportWrapper} from "../Styling/StatisticsBarStyling";
import {Results} from "../../../../types/src/conformanceCheckingTypes";

type ExportModalProps ={
    results: Results,
}

export const ExportModal = ({results} : ExportModalProps) =>{

    return(
        <p> Hello world</p>
    )
}

export const ExportContinueFunction = () => {

}